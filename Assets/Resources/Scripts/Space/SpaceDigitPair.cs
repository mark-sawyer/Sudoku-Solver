
public class SpaceDigitPair {
    public Space space { get; private set; }
    public Digit digit { get; private set; }

    public SpaceDigitPair(Space space, Digit digit) {
        this.space = space;
        this.digit = digit;
    }
}
